namespace JonathonOH.Freecell;

public static class GameFunctions
{
    /// <summary>
    /// Returns if move was successful.
    /// </summary>
    public static bool TryMakeMove(GameState initialState, out GameState resultState, Move move)
    {
        throw new NotImplementedException();
    }
}